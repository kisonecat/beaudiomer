Other than the prerequisites for beaudiomer (notably autocut, melt,
and pdflatex), you will need xmllint to run these tests.

On Debian and similar systems, it can be installed with `apt install libxml2-utils`

